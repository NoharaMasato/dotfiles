" this is a vim config file

" display number at left line
set number

" can undo
if has('persistent_undo')
  set undodir=~/.vim/undo
  set undofile                                                                                                                                   
endif


